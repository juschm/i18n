# -*- mode: python; -*- vim: set filetype=python:

for ts_lib in "parse_html parse_messages hashing quoting stringUtils newSet serializers/json serializer message_types config LoadingCache extractMessages Counter placeholderRegistryHintsForHtmlTags placeholderRegistryHints placeholderRegistry".split():
  typescript_to_nodejs(
      name="lib/{0}".format(ts_lib),
      srcs=["lib/{0}.ts".format(ts_lib)])

babeljs(name="lib/fingerprinting",
        srcs=["lib/fingerprinting.es6"])
